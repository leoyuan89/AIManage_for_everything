# 本地密码保险箱 v1 - 软件需求说明书

> 版本：v1.0
> 日期：2026-04-23
> 定位：完全本地化运行的个人密码与网址管理工具

---

## 1. 项目概述

### 1.1 产品定位

**本地密码保险箱**是一款运行在 Windows 平台的完全本地化个人密码与网址管理工具。核心架构遵循**"人工录入、AI治理"**的分工原则：

- **人工录入**：用户通过手动输入或本地 OCR 截图完成账号、网址等信息的录入，并保留人工校正权
- **AI治理**：本地部署的 Gemma 大模型专注于对已保存的结构化数据进行深度治理，不介入任何图像识别或密码解析

### 1.2 核心理念

- **完全离线**：除首次下载模型外，所有功能无需联网
- **隐私零上传**：任何账号数据、截图、搜索记录均不上传云端，大模型仅接触脱敏信息
- **AI辅助而非替代**：所有 AI 生成的分类、标签及整理建议均需经用户确认后方可写入数据库
- **决策权归属用户**：在享受智能管理便利的同时，确保隐私数据零上传、决策权完全归属用户

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        用户交互层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 密码管理  │  │ 网址管理  │  │ AI治理   │  │ 导入导出 │   │
│  │ - 录入   │  │ - 收藏   │  │ - 分类   │  │ - Excel │   │
│  │ - 编辑   │  │ - 分类   │  │ - 标签   │  │ - 备份  │   │
│  │ - 删除   │  │ - 去重   │  │ - 搜索   │  │ - PWA   │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
├─────────────────────────────────────────────────────────────┤
│                        业务逻辑层                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ Account  │  │ URL      │  │ Category │  │ Search   │   │
│  │ Service  │  │ Service  │  │ Service  │  │ Service  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ OCR      │  │ Export   │  │ Sync     │  │ Security │   │
│  │ Service  │  │ Service  │  │ Service  │  │ Service  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
├─────────────────────────────────────────────────────────────┤
│                        数据层                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                │
│  │ SQLite   │  │ AES加密  │  │ Config   │                │
│  │ 数据库   │  │ 引擎     │  │ 配置     │                │
│  └──────────┘  └──────────┘  └──────────┘                │
├─────────────────────────────────────────────────────────────┤
│                        AI层（本地）                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                 Ollama + Gemma 4B                     │   │
│  │  - 自动分类  - 生成标签  - 语义搜索  - 安全评估        │   │
│  │  - 批量整理  - 账号关联                                │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 功能模块

### 3.1 密码管理模块

#### 3.1.1 账号录入（人工主导）

| 编号 | 功能 | 说明 |
|------|------|------|
| P-01 | 手动录入 | 支持输入应用名、网址、账号、密码、备注 |
| P-02 | OCR截图导入 | 本地 PaddleOCR 识别截图文字，提取字段供用户校正 |
| P-03 | 人工校正 | 用户可对 OCR 结果进行修改、补充、取舍 |
| P-04 | 密码可见性切换 | 密码字段支持显示/隐藏切换 |
| P-05 | 保存确认 | 所有字段经用户确认后保存，AI不介入录入环节 |

#### 3.1.2 账号管理

| 编号 | 功能 | 说明 |
|------|------|------|
| P-06 | 查看详情 | 点击展开完整信息（应用名、网址、账号、密码、备注） |
| P-07 | 编辑修改 | 修改任意字段 |
| P-08 | 删除账号 | 单条删除，删除前需确认 |
| P-09 | 复制功能 | 一键复制账号、密码到剪贴板（密码复制后 20 秒自动清空） |
| P-10 | 分类筛选 | 左侧分类导航栏，点击分类筛选对应账号 |
| P-11 | 首字母排序 | 所有账号按应用名首字母（A-Z）排序展示 |
| P-12 | 最近添加 | 可选排序方式：最近添加优先 |

#### 3.1.3 AI治理（Gemma 4B）

**注意**：AI 仅对已保存的结构化数据进行治理，不接触密码明文。

| 编号 | 功能 | 说明 |
|------|------|------|
| P-13 | 自动分类 | 根据应用名+网址自动分类（金融/开发工具/社交平台/购物/其他） |
| P-14 | 生成标签 | 为账号生成关键词标签（如"支付"、"云服务"、"社交"） |
| P-15 | 一句话备注 | 自动生成账号用途的一句话描述 |
| P-16 | 安全等级评估 | 评估密码强度、是否重复、长期未更新等 |
| P-17 | 语义搜索 | 自然语言模糊检索，如"外卖账号"→美团，"跑模型的平台"→AutoDL |
| P-18 | 批量整理 | 定期自动修正零散账号的分类归属 |

| P-20 | 用户确认机制 | 所有 AI 生成的分类、标签、建议均需用户确认后写入 |

### 3.2 网址管理模块

#### 3.2.1 网址录入

| 编号 | 功能 | 说明 |
|------|------|------|
| U-01 | 添加网址 | 输入网址、标题、分类、备注 |
| U-02 | 快速添加 | 从密码库一键导入关联网址 |
| U-03 | 编辑修改 | 修改网址信息 |
| U-04 | 删除网址 | 单条删除 |

#### 3.2.2 网址管理

| 编号 | 功能 | 说明 |
|------|------|------|
| U-05 | 分类浏览 | 按分类查看网址收藏 |
| U-06 | 搜索查找 | 支持标题、网址、备注的关键词搜索 |
| U-07 | 智能去重 | AI 自动识别重复或相似的网址 |
| U-08 | 账号关联 | 同一平台下自动关联已存账号，查看密码时一键触达 |

#### 3.2.3 AI治理

| 编号 | 功能 | 说明 |
|------|------|------|
| U-09 | 自动分类 | 根据网址内容自动分类（如开发文档、云服务、社交平台） |
| U-10 | 语义搜索 | 如"深度学习平台"→AutoDL、Google Colab |
| U-11 | 关联推荐 | 保存 AutoDL 控制台时自动提示关联已存账号 |
| U-12 | 智能标签 | 生成技术栈、用途等标签 |

### 3.3 导入导出模块

| 编号 | 功能 | 说明 |
|------|------|------|
| I-01 | Excel 导出 | 导出所有账号到 .xlsx（可选含/不含密码） |
| I-02 | 加密备份 | 生成 .vault 加密备份文件，跨设备恢复 |
| I-03 | PWA 同步 | 生成加密 HTML 快照，手机离线查看 |
| I-04 | 批量导入 | 支持 Markdown/TXT/Excel 格式导入（MVP 3 后实现） |

---

## 4. AI 治理详细设计

### 4.1 数据边界

**AI 可以接触的信息**：
- 应用名、平台名
- 用户名（账号）
- 网址 URL
- 用户已填写的备注
- 分类标签

**AI 绝对不能接触的信息**：
- 密码明文（AES 加密存储）
- 原始截图文件
- 主密码及密钥派生信息

### 4.2 AI 工作流程

#### 4.2.1 自动分类流程

```
用户保存账号
    ↓
提取应用名 + 网址（脱敏）
    ↓
调用 Gemma："请判断该应用属于哪个分类"
    ↓
返回分类建议（如"开发工具"）
    ↓
展示给用户，等待确认
    ↓
用户点击"确认" → 写入数据库
用户点击"修改" → 用户手动选择
```

#### 4.2.2 语义搜索流程

```
用户输入："跑模型的平台"
    ↓
提取所有应用名列表
    ↓
构造 Prompt：
"用户在找什么账号？他说：'跑模型的平台'
可选列表：[美团, 微信, AutoDL, 支付宝, ...]
请返回匹配的应用名"
    ↓
Gemma 返回：AutoDL
    ↓
展示搜索结果 + 标注"AI 推荐"
```

### 4.3 Prompt 设计示例

**分类 Prompt**：
```
你是一个密码管理助手。请根据应用名称和网址，判断该账号属于哪个分类。

可选分类：金融、开发工具、社交平台、购物、云服务、娱乐、教育、其他

规则：
- 只返回分类名称，不要解释
- 开发工具包括：代码托管、IDE、API 平台、模型训练平台等
- 云服务包括：云服务器、云存储、CDN 等

应用名称：{app_name}
网址：{url}

分类：
```

**语义搜索 Prompt**：
```
用户正在搜索账号，他说："{query}"

密码库中的应用列表：
{app_list}

请从列表中找出用户最可能想找的应用，返回应用名。
只返回应用名，不要解释。如果没有匹配的，返回"无"。

匹配结果：
```

**安全评估 Prompt**：
```
请评估以下密码的安全等级（强/中/弱）：

密码：{password}

判断标准：
- 强：长度>=12，含大小写+数字+特殊字符
- 中：长度>=8，含两种字符类型
- 弱：其他情况

等级：
```

---

## 5. 技术架构

### 5.1 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| GUI 框架 | PyQt6 | Windows 桌面应用 |
| OCR 引擎 | PaddleOCR 2.7 | 本地截图文字识别（CPU 运行） |
| 本地大模型 | Ollama + Gemma 4B | HTTP API 异步调用 |
| 数据库 | SQLite | 本地加密存储 |
| 加密方案 | AES-256-GCM + PBKDF2 | 密码字段应用层加密 |
| 导出格式 | openpyxl（Excel） | 标准 xlsx 格式 |
| 手机同步 | HTML5 + WebCrypto API | PWA 离线解密 |

### 5.2 异步架构

```
用户操作（UI 线程）
    ↓
触发 AI 任务
    ↓
创建 QThread 后台线程
    ↓
线程中调用 Ollama API
    ↓
返回结果 → 信号槽机制更新 UI
    ↓
用户确认后写入数据库
```

**关键设计**：
- 所有 AI 推理任务在后台线程执行，避免 UI 卡顿
- 显示"AI 分析中..."加载状态
- 用户可随时取消 AI 建议，使用手动输入

### 5.3 数据库设计

**密码库数据库** (`vault_password.db`)：

```sql
-- 账号主表（敏感字段加密）
accounts
├── id: INTEGER PRIMARY KEY
├── app_name: TEXT          -- 应用名（明文）
├── url: TEXT               -- 网址（明文）
├── username: TEXT          -- 账号（加密）
├── password: TEXT          -- 密码（AES-256-GCM 加密）
├── category: TEXT          -- 分类（明文，AI 生成，用户确认）
├── tags: TEXT              -- 标签（JSON 数组，AI 生成 + 用户自定义）
├── remark: TEXT            -- 备注（加密）
├── ai_remark: TEXT         -- AI 生成的一句话备注（明文）
├── security_level: TEXT    -- 安全等级（强/中/弱，AI 评估）
├── last_password_change: TIMESTAMP  -- 最后修改密码时间
├── created_at: TIMESTAMP
├── updated_at: TIMESTAMP

-- AI 分类缓存
category_cache
├── app_name_hash: TEXT PRIMARY KEY
├── category: TEXT
├── hit_count: INTEGER
├── last_used: TIMESTAMP
```

**网址管理数据库** (`vault_urls.db`)：

```sql
-- 网址收藏表
urls
├── id: INTEGER PRIMARY KEY
├── title: TEXT             -- 标题
├── url: TEXT               -- 网址
├── category: TEXT          -- 分类
├── tags: TEXT              -- 标签（JSON 数组，AI 生成 + 用户自定义）
├── related_account_id: INTEGER  -- 关联的账号 ID（弱关联）
├── visit_count: INTEGER    -- 访问次数
├── created_at: TIMESTAMP
```

---

## 6. 隐私与安全设计

### 6.1 数据安全

- **密码加密**：所有密码使用 AES-256-GCM 加密，密钥由主密码通过 PBKDF2 派生
- **本地存储**：SQLite 数据库文件本地存储，不上传云端
- **AI 数据边界**：AI 仅接触应用名、用户名、网址等脱敏信息，绝不接触密码明文
- **截图清理**：OCR 完成后，原始截图文件默认不保留（可选保留）

### 6.2 用户控制权

- **AI 建议确认**：所有 AI 生成的分类、标签、建议均需用户点击"确认"后才生效
- **一键回滚**：用户可随时修改或删除 AI 生成的分类和标签
- **禁用 AI**：提供选项完全禁用 AI 功能，仅使用基础密码管理

### 6.3 会话安全

- **主密码机制**：首次启动设置主密码，用于派生加密密钥
- **会话锁定**：闲置 5 分钟后自动锁定，需重新输入主密码
- **剪贴板安全**：复制密码后 20 秒自动清空剪贴板

---

## 7. UI/UX 设计

### 7.1 整体风格

- 极简、无广告、无多余按钮
- 浅色/深色主题切换
- 界面参考：苹果备忘录 + 1Password 的简洁感

### 7.2 主界面布局

```
┌──────────────────────────────────────────────────────┐
│  [搜索框]              [+ 添加账号]  [⚙ 设置]         │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│  分类导航 │   A                                       │
│  ────────│   ┌─────────────────────────────────┐     │
│  ○ 全部  │   │ 支付宝         138****1234      │     │
│  ● 金融  │   │  alipay.com    [标签:支付,理财] │     │
│  ○ 社交  │   └─────────────────────────────────┘     │
│  ○ 开发  │                                           │
│  ○ 购物  │   B                                       │
│  ○ 其他  │   ┌─────────────────────────────────┐     │
│          │   │ 百度网盘       myname@163.com   │     │
│          │   │  pan.baidu.com [标签:云盘]      │     │
│          │   └─────────────────────────────────┘     │
│          │                                           │
├──────────┴───────────────────────────────────────────┤
│  [锁定]  [AI整理]  [导出]  [同步到手机]               │
└──────────────────────────────────────────────────────┘
```

### 7.3 账号详情弹窗

```
┌──────────────────────────────────────────┐
│ 支付宝                          [编辑] [🗑]│
│ alipay.com                               │
│ ──────────────────────────────────────── │
│ 账号: 138****1234             [复制]     │
│ 密码: ••••••••                [复制] [👁]│
│ 分类: 金融 ✨ (AI 推荐)                   │
│ 标签: 支付, 理财, 生活                    │
│ AI备注: 日常支付与理财平台               │
│ 安全: 🔒 强 (已 90 天未更新)             │
│ 备注: 绑定了银行卡，请勿修改密码         │
│ 关联网址: [查看账单] [转账] [余额宝]     │
│ 创建: 2026-01-15                         │
└──────────────────────────────────────────┘
```

---

## 8. 里程碑规划

### MVP 1：基础框架 + 加密存储 ✅
- PyQt6 界面框架
- SQLite + AES 加密
- 账号增删改查
- 主密码机制

### MVP 2：OCR + AI 分类 ✅
- PaddleOCR 截图导入
- Gemma 自动分类
- 分类缓存
- 用户确认机制

### MVP 3：搜索 + 导出 🔄
- 精确搜索（关键词）
- 语义搜索（自然语言）
- Excel 导出
- 加密备份

### MVP 4：网址管理 + 优化
- 网址收藏模块
- 账号-网址关联
- 智能去重
- UI 美化、主题切换

### MVP 5：AI 深度治理
- 标签生成（支持用户自定义）
- 一句话备注
- 安全等级评估
- 批量整理

### MVP 6：PWA 同步 + 完善
- 手机离线同步
- 批量导入
- 性能优化
- 完善文档

---

## 9. 非功能需求

| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-01 | 完全离线 | 核心功能无需联网 |
| NFR-02 | 隐私零上传 | 任何数据不上传云端 |
| NFR-03 | 响应速度 | AI 分类接口响应 < 1 秒（4060 本地推理） |
| NFR-04 | 并发支持 | 单用户本地使用，无需多用户并发 |
| NFR-05 | 兼容性 | Windows 10/11，支持 x64 架构 |
| NFR-06 | 显存占用 | AI 模块运行时显存占用 < 7GB |
| NFR-07 | 数据持久化 | 数据库文件本地存储，重装后通过备份恢复 |

---

## 10. 待确认问题

1. ✅ **开发顺序**：先完成密码库，再开发网址管理
2. ✅ **标签系统**：支持 AI 生成 + 用户自定义
3. ❌ **密码生成器**：不内置
4. ✅ **数据库分离**：密码库和网址管理使用独立数据库文件
5. **安全评估**：密码强度评估是否需要在 UI 中展示？

---

*本文档作为 v1 版本的开发指导，后续根据实际开发情况迭代更新。*
