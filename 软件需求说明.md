

# 密码管理软件需求说明书（SRS）

## 1. 项目概述

### 1.1 项目名称
**本地密码保险箱（Local Password Vault）**

### 1.2 目标
开发一款 Windows 平台下的本地密码管理软件，采用本地大模型（Gemma 4 E4B）实现智能分类与自然语言搜索，所有数据本地加密存储，不上传、不登录，支持截图 OCR 提取与手机离线同步。

### 1.3 用户画像
- 对个人隐私极度敏感，不信任云端密码管理器
- 希望"手写记录"级别的安全性，同时具备数字化管理的便利性
- 账号数量多，需要快速检索与分类

---

## 2. 功能需求（Functional Requirements）

### 2.1 账号密码管理（核心）

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-01 | 添加账号 | 支持手动输入（应用名/网址、账号、密码、备注） |
| FR-02 | 截图导入 | 上传截图 → OCR 提取文字 → 自动填充账号/密码字段 → 用户确认修正 |
| FR-03 | 编辑账号 | 修改任意字段（应用名、网址、账号、密码、备注、分类） |
| FR-04 | 删除账号 | 单条删除，删除前需确认 |
| FR-05 | 查看详情 | 点击列表项展开详情页，密码默认隐藏，点击显示/隐藏切换 |
| FR-06 | 复制功能 | 一键复制账号、一键复制密码到剪贴板（复制后 10 秒自动清空剪贴板） |

### 2.2 AI 智能分类（Gemma 4 E4B 集成）

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-07 | 自动分类 | 新增/编辑账号时，自动调用本地 Gemma 模型，根据应用名+网址返回分类（社交/金融/邮箱/游戏/工作/其他） |
| FR-08 | 分类修正 | 用户可手动修改 AI 分类结果，修正后可选"记住此应用名的分类"（本地规则缓存） |
| FR-09 | 批量分类 | 对已有未分类记录，可一键触发 AI 批量分类 |
| FR-10 | 分类展示 | 左侧/顶部分类导航栏，点击分类筛选对应账号 |

### 2.3 搜索与排序

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-11 | 首字母排序 | 所有账号按应用名首字母（A-Z）排序展示 |
| FR-12 | 自然语言搜索 | 搜索框支持语义搜索（如"找我的游戏账号"、"支付宝那个"），调用 Gemma 理解意图并返回匹配结果 |
| FR-13 | 关键词搜索 | 支持应用名、网址、账号、备注的常规模糊匹配 |
| FR-14 | 最近添加 | 可选排序方式：最近添加优先 |

### 2.4 数据安全与加密

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-15 | 主密码机制 | 首次启动设置主密码，用于派生 AES-256 加密密钥（PBKDF2，10 万次迭代） |
| FR-16 | 数据库加密 | 所有账号数据以 AES-256-GCM 加密后存储于本地 SQLite |
| FR-17 | 会话锁定 | 闲置 5 分钟后自动锁定，需重新输入主密码解锁 |
| FR-18 | 截图不保留 | OCR 提取完成后，原始截图文件可选择不保留（仅保留提取后的文本） |

### 2.5 导入导出

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-19 | Excel 导出 | 一键导出所有账号到 Excel（.xlsx），导出时可选择是否包含密码 |
| FR-20 | 加密备份 | 生成加密备份文件（.vault），可用于跨设备恢复 |
| FR-21 | 手机同步 | 生成加密 HTML 快照（PWA），传输到手机后离线查看 |

### 2.6 手机离线同步（PWA）

| 编号 | 功能 | 说明 |
|------|------|------|
| FR-22 | 生成密包 | 电脑端一键生成加密 HTML 文件，内含所有账号数据 |
| FR-23 | 手机查看 | 手机浏览器打开 HTML → 输入主密码 → 本地解密查看 |
| FR-24 | 添加到主屏幕 | 手机可将 PWA 添加到桌面，像原生 App 一样全屏运行 |
| FR-25 | 单向同步 | 手机端只读，修改必须回电脑操作；重新生成密包覆盖旧版 |

---

## 3. 非功能需求（Non-Functional Requirements）

| 编号 | 需求 | 说明 |
|------|------|------|
| NFR-01 | 完全离线 | 核心功能（除首次下载模型外）无需联网 |
| NFR-02 | 隐私零上传 | 任何账号数据、截图、搜索记录均不上传云端 |
| NFR-03 | 响应速度 | AI 分类接口响应 < 1 秒（4060 本地推理） |
| NFR-04 | 并发支持 | 单用户本地使用，无需多用户并发 |
| NFR-05 | 兼容性 | Windows 10/11，支持 x64 架构 |
| NFR-06 | 显存占用 | AI 模块运行时显存占用 < 7GB，与 YOLO 等其他 GPU 任务可分时复用 |
| NFR-07 | 数据持久化 | 数据库文件本地存储，重装软件后通过备份文件可恢复 |

---

## 4. 界面需求（UI/UX）

### 4.1 整体风格
- 极简、无广告、无多余按钮
- 深色/浅色主题切换
- 界面参考：苹果备忘录 + 1Password 的简洁感

### 4.2 页面结构
```
┌─────────────────────────────────────┐
│  搜索框                    [+]添加   │
├──────────┬──────────────────────────┤
│ 分类导航  │  账号列表（首字母索引）    │
│ · 全部   │  ┌────────────────────┐  │
│ · 金融   │  │ A 支付宝           │  │
│ · 社交   │  │   alipay.com       │  │
│ · 游戏   │  │ B 百度网盘         │  │
│ · ...    │  │   pan.baidu.com    │  │
│          │  │ C ...              │  │
│          │  └────────────────────┘  │
├──────────┴──────────────────────────┤
│  设置 | 锁定 | 导出 | 同步到手机     │
└─────────────────────────────────────┘
```

### 4.3 添加/编辑弹窗
- **标签页切换**：手动输入 / 截图导入
- **截图导入流程**：上传 → OCR 提取 → 自动填充 → 用户确认 → AI 分类 → 保存
- **字段**：应用名（必填）、网址（选填）、账号（必填）、密码（必填，可显示/隐藏）、备注（选填）、分类（AI 自动填充，可修改）

---

## 5. 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| **GUI 框架** | PyQt6 / PySide6 | 跨平台、组件丰富、可定制主题 |
| **OCR 引擎** | PaddleOCR | 中文场景识别率高，CPU 运行 |
| **本地大模型** | Ollama + Gemma 4 E4B | HTTP API 调用，模型可热切换 |
| **数据库** | SQLite + SQLCipher | 本地加密存储，单文件便于备份 |
| **加密方案** | AES-256-GCM + PBKDF2 | 主密码派生密钥，10 万次哈希 |
| **导出格式** | openpyxl（Excel） | 标准 xlsx 格式 |
| **手机同步** | HTML5 + WebCrypto API | PWA 离线解密，浏览器原生 AES |

---

## 6. 数据模型（核心表结构）

```sql
-- 账号主表（加密存储）
accounts
├── id: INTEGER PRIMARY KEY
├── app_name: TEXT          -- 应用名（加密）
├── url: TEXT               -- 网址（加密）
├── username: TEXT          -- 账号（加密）
├── password: TEXT          -- 密码（加密）
├── category: TEXT          -- 分类：社交/金融/邮箱/游戏/工作/其他
├── remark: TEXT            -- 备注（加密）
├── created_at: TIMESTAMP
└── updated_at: TIMESTAMP

-- AI 分类缓存（提升重复应用名的分类速度）
category_cache
├── app_name: TEXT PRIMARY KEY
├── category: TEXT
└── hit_count: INTEGER      -- 命中次数，用于优化排序
```

---

## 7. AI 交互流程定义

### 7.1 自动分类流程
```
用户输入/确认应用名 + 网址
        ↓
Python 检查 category_cache 是否有记录
        ↓
有 → 直接返回缓存分类
无 → 调用 Ollama API (Gemma 4 E4B)
        ↓
Gemma 返回分类词（如"金融"）
        ↓
Python 写入 category_cache 并展示给用户
        ↓
用户可手动修正，修正后更新缓存
```

### 7.2 自然语言搜索流程
```
用户输入搜索词（如"我的游戏账号"）
        ↓
Python 获取所有账号的应用名+网址列表
        ↓
构造 Prompt 发给 Gemma
        ↓
Gemma 返回匹配的应用名列表
        ↓
Python 根据返回结果从数据库提取完整记录展示
```

---

## 8. 里程碑与开发顺序

| 阶段 | 周期 | 交付物 |
|------|------|--------|
| **MVP 1** | 1 周 | PyQt 界面框架 + 手动增删改查 + SQLite 加密存储 |
| **MVP 2** | 1 周 | PaddleOCR 截图导入 + AI 自动分类集成 |
| **MVP 3** | 3-5 天 | 首字母排序 + 自然语言搜索 + Excel 导出 |
| **MVP 4** | 3-5 天 | 手机 PWA 同步 + 加密 HTML 生成 |
| **优化** | 持续 | UI 美化、分类缓存优化、弱密码检测 |

---

**这份说明书可以直接作为你开发时的功能 checklist。** 需要我现在就开始写 MVP 1 的 PyQt 框架代码吗？