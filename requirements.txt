# 本地密码保险箱 - Python 依赖包
# 注意：Ollama 需单独安装（https://ollama.com），不属于 pip 包

# ==================== GUI 框架 ====================
PyQt6>=6.4.0
qt-material>=2.14
qtawesome>=1.2.0

# 应用方式：
# from qt_material import apply_stylesheet
# apply_stylesheet(app, theme='light_blue.xml')  # 浅色 Material 主题

# ==================== OCR 引擎 ====================
# PaddleOCR 及其底层推理引擎（CPU 版本）
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# ==================== 数据库与加密 ====================
# 应用层加密方案：sqlite3(标准库) + cryptography
# 无需 SQLCipher，避免 Windows 编译问题

cryptography>=41.0.0

# ==================== 数据导出 ====================
# Excel 导出 (.xlsx)
openpyxl>=3.1.0

# ==================== 网络请求 ====================
# 调用本地 Ollama API
requests>=2.31.0

# ==================== 工具库 ====================
# 剪贴板操作（复制账号/密码）
pyperclip>=1.8.0
# 图像处理（OCR 前处理、截图处理）
Pillow>=10.0.0
# HTML 模板引擎（生成 PWA 离线同步文件）
Jinja2>=3.1.0

# 拼音转换（中文搜索）
pypinyin>=0.50.0
